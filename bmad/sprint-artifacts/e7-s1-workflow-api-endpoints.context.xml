<story-context id=".bmad/bmm/workflows/4-implementation/story-context/e7-s1-workflow-api-endpoints" v="1.0">
  <metadata>
    <epicId>7</epicId>
    <storyId>1</storyId>
    <title>Story 7.1: Workflow API Endpoints</title>
    <status>drafted</status>
    <generatedAt>2025-11-28</generatedAt>
    <generator>BMAD Story Context Workflow</generator>
    <sourceStoryPath>docs/sprint-artifacts/e7-s1-workflow-api-endpoints.md</sourceStoryPath>
  </metadata>

  <story>
    <asA>Holokai Desktop backend developer</asA>
    <iWant>REST API endpoints for workflow management (CRUD</iWant>
    <soThat>the desktop client can persist and retrieve workflow definitions and execution records from the Moku API backend.</soThat>
    <tasks>

    </tasks>
  </story>

  <acceptanceCriteria>
    <criterion id="1">**AC-1.1**: `GET /api/workflows` returns paginated workflow list with filters (scope, projectId)</criterion>
    <criterion id="2">**AC-1.2**: `GET /api/workflows/{id}` returns workflow with full definition (inputs, steps, outputs, capabilities)</criterion>
    <criterion id="3">**AC-1.3**: `POST /api/workflows` creates workflow with validation (required fields, JWT owner_id)</criterion>
    <criterion id="4">**AC-1.4**: `PATCH /api/workflows/{id}` updates workflow (only owner or admin can update)</criterion>
    <criterion id="5">**AC-1.5**: `DELETE /api/workflows/{id}` soft deletes workflow (sets deleted_at timestamp)</criterion>
    <criterion id="6">**AC-1.6**: `POST /api/workflows/{id}/fork` creates copy with forked_from reference</criterion>
    <criterion id="7">**AC-1.7**: `POST /api/workflows/{id}/execute` creates execution record (does NOT run synchronously)</criterion>
    <criterion id="8">**AC-1.8**: `GET /api/workflows/{id}/executions` returns paginated execution history with status filter</criterion>
    <criterion id="9">**AC-1.9**: All endpoints enforce RBAC (only owner/admin can edit, delete; all project members can view)</criterion>
  </acceptanceCriteria>

  <artifacts>
    <docs>
      <techSpec>docs/sprint-artifacts/tech-spec-epic-7.md</techSpec>
    </docs>
    <code>
      <!-- Relevant existing code files will be identified during implementation -->
    </code>
    <dependencies>
      <dependency>Epic 7 Tech Spec</dependency>
    </dependencies>
  </artifacts>

  <constraints>
    <constraint>Desktop application architecture (Electron multi-process)</constraint>
    <constraint>IPC communication patterns between main and renderer processes</constraint>
    <constraint>State management using Svelte stores</constraint>
    <constraint>TypeScript for type safety</constraint>
  </constraints>

  <interfaces>
    <!-- Key interfaces and API signatures will be documented during implementation -->
  </interfaces>

  <tests>
    <standards>
      <standard>Unit tests using Vitest</standard>
      <standard>Integration tests for IPC communication</standard>
      <standard>E2E tests using Playwright</standard>
      <standard>Target 85%+ code coverage</standard>
    </standards>
    <locations>
      <location>Unit tests: src/**/*.test.ts</location>
      <location>Integration tests: tests/integration/**/*.test.ts</location>
      <location>E2E tests: tests/e2e/**/*.spec.ts</location>
    </locations>
    <ideas>
      <idea ac="1">Test: **AC-1.1**: `GET /api/workflows` returns paginated workflow list with filters (s...</idea>
      <idea ac="2">Test: **AC-1.2**: `GET /api/workflows/{id}` returns workflow with full definition (inp...</idea>
      <idea ac="3">Test: **AC-1.3**: `POST /api/workflows` creates workflow with validation (required fie...</idea>
      <idea ac="4">Test: **AC-1.4**: `PATCH /api/workflows/{id}` updates workflow (only owner or admin ca...</idea>
      <idea ac="5">Test: **AC-1.5**: `DELETE /api/workflows/{id}` soft deletes workflow (sets deleted_at ...</idea>
    </ideas>
  </tests>
</story-context>