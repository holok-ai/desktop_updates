A recent change to the dependency list is causing js-yaml to be used as a referenced package .
js-yaml has a critical security flaw and should be avoided.

This ticket is for the following purpose:

identify what package or packages are referencing js-yaml.
provide options for not using the package that references js-yaml
The output of this task is not code.
The output of this task is a set of between 1 and 4 options.
Each option should include a description, the method of resolution, estimated code size impact, advantages and disadvantages.

The output should be no more than 100 lines long and should not include code, code snippets or sample code.
